{
  "name": "@fnclaude/cli",
  "version": "1.1.1",
  "description": "fnclaude CLI implementation (TypeScript rewrite, in progress)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fnclaude/fnclaude.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "main": "./src/main.ts",
  "bin": {
    "fnc": "./bin/fnc.js"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "lint": "echo 'lint stub (cli, rewrite window)' && exit 0",
    "test": "echo 'tests skipped during rewrite window' && exit 0",
    "build": "echo 'no build step (bun runs ts directly)' && exit 0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "dependencies": {}
}
